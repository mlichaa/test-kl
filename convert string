const optionRule = '{1069} AND ({1070} OR {1071} OR {1072}) AND {1244} AND ({1245} OR {1339})';

function convertSt(optionRule)
{
  optionRule = optionRule.split(" AND ");
  optionRule.forEach(element => {
    console.log(element);
  });
}


convertSt(optionRule);
 
